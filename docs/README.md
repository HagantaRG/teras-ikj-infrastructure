# Project documentation

Keep sheet schemas, workflow descriptions, and operating instructions here.
Document tab names, expected columns, entry point functions, required services,
and trigger schedules as each automation is developed.

The existing `createFormFromSheet` function is in `src/automations/Code.js`.
It reads the `stok-barang` tab, creates a Google Form, and logs its edit URL.

Use synthetic examples when documenting sheet data.
